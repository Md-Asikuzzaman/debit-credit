import { NextPage } from "next";

interface Props {}

const AddTransaction: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>AddTransaction</h1>
    </div>
  );
};

export default AddTransaction;
